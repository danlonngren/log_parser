[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "log-parser"
version = "0.1.0"
dependencies = []

[project.scripts]
log-parser = "log_parser.cli:main"